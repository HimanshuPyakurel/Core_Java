package Collection;

public class CollectionDocs {

	/*
	 *  =============== Collection Framework / Java Collection / DS ====================
	 *  # Use to store and process dynamic multiple data/object
	 *  
	 *  1> List : dynamic array
	 *      a> ArrayList : slow processing, better to use for data storage
	 *      b> LinkedList : fast processing, better to use for data processing
	 *  
	 *  2> Set : collection of unique data.
	 *      a> HastSet       : Unique data only 
	 *      b> TreeSet       : unique + sorted data
	 *      c> LinkedHashSet : unique + insertion in oder (show data as per the insertion)
	 *      
	 *  3> Map : data manage in key,value pair <k,v>
	 *      a> HashMap       : <k,v> pair 
	 *      b> TreeMap       : <k,v> + sorted by key
	 *      c> LinkedHashMap : <k,v> + insertion in order as per key
	 *      
	 */
	
	
	
}
